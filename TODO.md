# TODOs

- [ ] Revise the Calendar Monthly View by first fetching all COMMITs from all repos in the past month, cache them, and then display them in the calendar view so that each COMMIT shows up within the day that was created.
- [ ] Optimize the data fetching process to reduce the number of API calls.
- [ ] Add a way to configure the GitHub repositories to fetch data from.
- [ ] Implement error handling for network requests.
- [ ] Create a user interface for displaying the fetched data.
- [ ] Write unit tests for the data fetching functions.